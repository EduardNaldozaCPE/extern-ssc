class Page{
    constructor(){
        this.list = [];
    }
    initpage = () => {
        if (!(canvas.width <= canvas.height+(canvas.width/10) || canvas.width <= 1000)) {
            
        } else {
            // mobileUI()
        }

    }
    startInstanceList = () => {

        return this.list;
    }
    
    action = () => {
    }
}